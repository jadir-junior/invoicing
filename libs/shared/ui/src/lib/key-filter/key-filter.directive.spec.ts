import { KeyFilterDirective } from './key-filter.directive';

describe('KeyFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyFilterDirective();
    expect(directive).toBeTruthy();
  });
});
